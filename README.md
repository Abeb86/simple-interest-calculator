# Simple Interest Calculator

This project contains a simple Bash script to calculate simple interest.

The calculator takes the principal amount, rate of interest, and time period
as input and computes the simple interest using the formula:

Simple Interest = (Principal × Rate × Time) / 100
